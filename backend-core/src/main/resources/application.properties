server.port=${PORT:8080}

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5435/knust_pulse}
spring.datasource.username=${DATABASE_USER:knust}
spring.datasource.password=${DATABASE_PASSWORD:knust123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

spring.jackson.serialization.write-dates-as-timestamps=false

moderation.service.url=${MODERATION_SERVICE_URL:http://localhost:8001}
jwt.secret=${JWT_SECRET:knust-pulse-dev-secret-key-min-256-bits-long-for-hs256!!}
jwt.expiration-ms=86400000
server.error.include-message=always
